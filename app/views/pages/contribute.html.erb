<h1> Contribute </h1>
<h4> Quotes you created: </h4>
<ol>
  <% @user.quotes.each do |quote| %>
    <li> <%= quote.politician.first_name %> <%= quote.politician.last_name %>: "<%= quote.content %>" <%= link_to "Source" , "https://" + quote.source_link %> <%= quote.source_tracker ? "hasTracker" : "hasNoTracker" %> </li>
  <% end %>
</ol>

<%= link_to "Create quote", new_quote_path %>
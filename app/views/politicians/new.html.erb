<%= simple_form_for [ @politician ] do |f| %> 
  <%= f.input :first_name %> 
  <%= f.input :last_name %> 
  <%= f.input :party %> 
  <%= f.association :location, label_method: :name, value_method: :id, include_blank: false %>
  <%= f.submit "Add Politician", class: "btn btn-primary" %> 
<% end %>